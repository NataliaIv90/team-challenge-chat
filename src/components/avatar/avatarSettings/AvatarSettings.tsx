import styles from './AvatarSettings.module.scss';

export const AvatarSettings = () => {
  const sectionClassname = styles['section'];

  return (
    <section className={sectionClassname}>AvatarSettings</section>
  )
}